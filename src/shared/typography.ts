/** Typography scale (loaded via Google Fonts in index.html — zero bundle weight). */
export const typography = {
  /** Editorial/story voice — Cormorant Garamond. */
  editorial: 'Cormorant Garamond',
  /** Technical/data voice — JetBrains Mono. */
  mono: 'JetBrains Mono',
} as const